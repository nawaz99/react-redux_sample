const initialState = {
    cocktails:[],
    loading:false,
    error:null
};

const cockTailReducer = (state = initialState,action)=>{
    switch(action.type){
        default:return state;
    }
};

export default cockTailReducer;

